# Architecture

Document system boundaries, major components, data flows, integrations, trust
boundaries, deployment topology, and ownership here when the project needs
architecture guidance.

Use `/analyze-project` to describe evidence-backed current state for an existing
system. Use `/design-architecture <scope>` only when the user explicitly requests
architecture design or redesign. Record consequential accepted decisions as
ADRs; do not present proposed architecture as current implementation.
